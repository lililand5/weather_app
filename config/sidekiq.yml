development:
  :concurrency: 1
:queues:
  - default
# :max_retries: 10

:schedule:
  check_match_every_hour:
    cron: '*/5 * * * * *'   # Runs once per 1 hour
    class: HardJob
    description: "Runs every hour. Сheck matches, witch have not been parsed. Put an asynchronous task on parsing queue"
